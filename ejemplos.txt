var (
    $mif = function($x, $y) ($x + $y)
)
in
    eval($mif 5 10)


rec 
    $mif($x, $y) = 
        cond
            [compare($y == 1) 1]
            else
                ($x * eval($mif $x ($y ~ 1)))
in
    eval($mif 2 5)
end


rec
	$factorial($x)=  
	    cond
	      [compare($x == 0) 1]
	      [compare($x == 1) 1]
	    else
	      ($x * eval($factorial ($x ~ 1)))
in
	eval($factorial 5)
end


